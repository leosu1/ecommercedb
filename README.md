database structure dans le fichier "ecommerceapp.sql"\
données à insérer dans la bdd dans le fichier "ecommerceappdump.sql"\
querries fonctionnelles pour tester les données dans le fichier "queries.sql"
